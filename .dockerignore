# Exclude IDE, build artifacts, and secrets from the Docker build context
.git
.gitignore
.idea
*.iml
*.iws
out/
target/
.freebuff/
# Never send real env files (with secrets) into the build context
.env
